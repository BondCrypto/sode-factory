{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "gate verdict JSON",
  "description": "PLACEHOLDER schema (C0/S1): gives migrate + doctor a registry entry to walk. The owning component C7 (S6) replaces this with the real shape.",
  "type": "object",
  "required": ["schema_version"],
  "properties": {
    "schema_version": { "const": 1 }
  },
  "additionalProperties": true
}
