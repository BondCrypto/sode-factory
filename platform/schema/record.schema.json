{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "work/<id>/record.yaml",
  "description": "PLACEHOLDER schema (C0/S1): gives migrate + doctor a registry entry to walk. The owning component C6 (S8) replaces this with the real shape.",
  "type": "object",
  "required": ["schema_version"],
  "properties": {
    "schema_version": { "const": 1 }
  },
  "additionalProperties": true
}
