# frontend KB — seeded in its W1-W2 seeding session (S9 / build-plan B018). Empty at S1.
# KB entry shape: architecture doc section 4A. A model never writes best practices from memory; every entry names a source.
