header:
  repo: sode-factory
  generated_from:
  - docs/design/sode_factory_build_sessions.md
  - docs/design/sode_factory_build_plan.md §3
  generated_on: '2026-09-21'
  notes: sode-scoped backlog. Sessions S1–S21 + K1–K7 are the v1 build; seeds B001–B025 are deferred/design items.
    Edit here; render later via a sode gen script (C0).
items:
- id: session-s1-skeleton
  kind: build_session
  session: S1
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S1 — skeleton: C0: layout · `factory.yaml` schema · CLI registry · `sode init/doctor/version/migrate`
    · `.sode/local.yaml` · minimal `a'
  description: 'Build session S1 (skeleton). Builds: C0: layout · `factory.yaml` schema · CLI registry · `sode init/doctor/version/migrate`
    · `.sode/local.yaml` · minimal `active.json` · `platform/ports/` manifests · standalone test (skeleton form).
    Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: '`make test` green in a temp HOME · `doctor` 0 in `local` profile · BAD schema fixture refused · `migrate
      --check` round-trip'
  depends_on:
  - session-s0
- id: session-s2-work-store
  kind: build_session
  session: S2
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S2 — work store: C1: item schema · `classes.yaml` (generated tables) · routing · labels-as-holds · scheduler
    · leases on disk · `sode new'
  description: 'Build session S2 (work store). Builds: C1: item schema · `classes.yaml` (generated tables) · routing
    · labels-as-holds · scheduler · leases on disk · `sode new/intake/ready/hold/release/dep/lint` · triage output
    schema. Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: tables byte-equal to render · depth fixtures incl. medium-D1/D2 · lease + WIP fixtures · triage schema
      fixture
  depends_on:
  - session-s1-skeleton
- id: session-s3-guards-red
  kind: build_session
  session: S3
  priority: P1
  lane: RED
  status: pending
  title: 'S3 — guards (RED): C4: `sode-sh` · `sode-run --actor` · rules from the seed catalogue with provenance
    · `git.yaml` · `secrets.yaml` · hook '
  description: 'Build session S3 (guards (RED)). Builds: C4: `sode-sh` · `sode-run --actor` · rules from the seed
    catalogue with provenance · `git.yaml` · `secrets.yaml` · hook installers · hash-chained shim log · `verify-log`
    · evaluator-boundary denies · path rules. Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: known-bad corpus all denied · known-good allowed · monotone deny test · bypass-proof matrix (mock) ·
      `verify-log` tamper test
  depends_on:
  - session-s1-skeleton
- id: session-s4-worker-runtime
  kind: build_session
  session: S4
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S4 — worker runtime: C3: provisioner (fresh clone per stage · read-only plan clone · `branch_ref`/`dispatch_ref`)
    · watchdog · mock adapter ·'
  description: 'Build session S4 (worker runtime). Builds: C3: provisioner (fresh clone per stage · read-only plan
    clone · `branch_ref`/`dispatch_ref`) · watchdog · mock adapter · adapter registry + capability suite · secrets
    injection · redaction · services supervisor · disk watermark · quarantine. Spec: docs/design/sode_factory_component_specs.md;
    order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: clone isolation · env-scrub AST · mock end-to-end · positive-secret redaction · watchdog kill/quarantine
      · DEGRADED on cli_drift
  depends_on:
  - session-s1-skeleton
  - session-s3-guards-red
- id: session-s5-brief
  kind: build_session
  session: S5
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S5 — brief: C5: renderer (stable prefix · recitation · empty-KB marker) · ONE `sode lint` · spec-lock
    brief · report-back schema · s'
  description: 'Build session S5 (brief). Builds: C5: renderer (stable prefix · recitation · empty-KB marker) ·
    ONE `sode lint` · spec-lock brief · report-back schema · spec-hash pin. Spec: docs/design/sode_factory_component_specs.md;
    order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: determinism · size budget · provider-neutral · seven-part refusal · re-lock on spec change · poisoned
      fixtures fail
  depends_on:
  - session-s2-work-store
- id: session-s6-gate-core
  kind: build_session
  session: S6
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S6 — gate core: C7a: registry · cache · T0/T1 · toolchain checks · anti-gaming checks · `sode-run` execution
    · determinism guard · escap'
  description: 'Build session S6 (gate core). Builds: C7a: registry · cache · T0/T1 · toolchain checks · anti-gaming
    checks · `sode-run` execution · determinism guard · escape-log four-part record · gate-outcomes ledger. Spec:
    docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: remediation on every check · BAD fixture per check · 0-checks = FAIL · anti-gaming fixtures fail T0
      · `--repeat 10 --frozen` identical
  depends_on:
  - session-s1-skeleton
  - session-s2-work-store
  - session-s3-guards-red
- id: session-s7-engine-red
  kind: build_session
  session: S7
  priority: P1
  lane: RED
  status: pending
  title: 'S7 — engine (RED): C2: state machine + invariant table · leased ledger · actions ledger · effects outbox
    · classify (trailers · clean tree '
  description: 'Build session S7 (engine (RED)). Builds: C2: state machine + invariant table · leased ledger · actions
    ledger · effects outbox · classify (trailers · clean tree · acceptance hash) · review fan-out barrier · replan
    edge · clock · `sode tick [--until]/status`. Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: transition + invariant tables · double-fire · duplicate action_id · corrupt=FATAL · PARTIAL continuation
      · child HALT → replan · commit-count trap fixture
  depends_on:
  - session-s2-work-store
  - session-s4-worker-runtime
  - session-s5-brief
- id: session-s8-record
  kind: build_session
  session: S8
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S8 — record: C6: events.jsonl → projection · redaction before write · two completeness gates · hash-bound
    decisions + actors · `ship_'
  description: 'Build session S8 (record). Builds: C6: events.jsonl → projection · redaction before write · two
    completeness gates · hash-bound decisions + actors · `ship_hash` · narrative · PR view (mock GitHub). Spec:
    docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: golden projection · no report-back → PARTIAL · untrusted approve = note · hash invalidation · for_ship
      vs after_measure
  depends_on:
  - session-s7-engine-red
  - session-s5-brief
- id: session-s9-departments
  kind: build_session
  session: S9
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S9 — departments: C9: loader · seven contract dirs · KB entry schema · `kb_ready` gate · `sode kb seed/gaps`
    · seats + handoff notes · fre'
  description: 'Build session S9 (departments). Builds: C9: loader · seven contract dirs · KB entry schema · `kb_ready`
    gate · `sode kb seed/gaps` · seats + handoff notes · freshness. Spec: docs/design/sode_factory_component_specs.md;
    order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: contract check · kb_ready fixture pair · unready route holds · seed round-trip · ten QA angles doc-sync
  depends_on:
  - session-s2-work-store
- id: session-k1-security-kb
  kind: kb_seeding_session
  session: K1
  priority: P1
  lane: YELLOW
  status: pending
  title: 'K1 — security KB: seed the department KB'
  description: 'KB seeding session K1 (security KB). Seeds: our guard rules · pre-flight discipline · CVE-class
    patterns · tooling picks (pinned). Process: architecture §4A (sourced entries, kb_ready gate).'
  acceptance:
  - cmd: sode kb gaps
    expect: '`kb_ready(security)` true'
  depends_on:
  - session-s1-skeleton
- id: session-k2-qa-quality-kb
  kind: kb_seeding_session
  session: K2
  priority: P1
  lane: YELLOW
  status: pending
  title: 'K2 — qa_quality KB: seed the department KB'
  description: 'KB seeding session K2 (qa_quality KB). Seeds: the ten angles · lean bar · anti-gaming rules · refactor
    patterns. Process: architecture §4A (sourced entries, kb_ready gate).'
  acceptance:
  - cmd: sode kb gaps
    expect: '`kb_ready(qa_quality)` true'
  depends_on:
  - session-s1-skeleton
- id: session-k3-factory-ops-kb
  kind: kb_seeding_session
  session: K3
  priority: P1
  lane: YELLOW
  status: pending
  title: 'K3 — factory_ops KB: seed the department KB'
  description: 'KB seeding session K3 (factory_ops KB). Seeds: two-currencies · cache discipline · harness lessons
    · metrics definitions. Process: architecture §4A (sourced entries, kb_ready gate).'
  acceptance:
  - cmd: sode kb gaps
    expect: '`kb_ready(factory_ops)` true — REQUIRED before S10'
  depends_on:
  - session-s1-skeleton
- id: session-k4-backend-kb
  kind: kb_seeding_session
  session: K4
  priority: P1
  lane: YELLOW
  status: pending
  title: 'K4 — backend KB: seed the department KB'
  description: 'KB seeding session K4 (backend KB). Seeds: operator-brought articles + best practices, distilled
    with citations. Process: architecture §4A (sourced entries, kb_ready gate).'
  acceptance:
  - cmd: sode kb gaps
    expect: '`kb_ready(backend)` true'
  depends_on:
  - session-s1-skeleton
- id: session-k5-frontend-kb
  kind: kb_seeding_session
  session: K5
  priority: P1
  lane: YELLOW
  status: pending
  title: 'K5 — frontend KB: seed the department KB'
  description: 'KB seeding session K5 (frontend KB). Seeds: operator-brought articles + best practices. Process:
    architecture §4A (sourced entries, kb_ready gate).'
  acceptance:
  - cmd: sode kb gaps
    expect: '`kb_ready(frontend)` true'
  depends_on:
  - session-s1-skeleton
- id: session-k6-devops-kb
  kind: kb_seeding_session
  session: K6
  priority: P1
  lane: YELLOW
  status: pending
  title: 'K6 — devops KB: seed the department KB'
  description: 'KB seeding session K6 (devops KB). Seeds: operator-brought articles + best practices (CI/CD · envs
    · runtime hosts). Process: architecture §4A (sourced entries, kb_ready gate).'
  acceptance:
  - cmd: sode kb gaps
    expect: '`kb_ready(devops)` true'
  depends_on:
  - session-s1-skeleton
- id: session-k7-architecture-kb
  kind: kb_seeding_session
  session: K7
  priority: P1
  lane: YELLOW
  status: pending
  title: 'K7 — architecture KB: seed the department KB'
  description: 'KB seeding session K7 (architecture KB). Seeds: decision-record template · design sources · the
    elegance bar. Process: architecture §4A (sourced entries, kb_ready gate).'
  acceptance:
  - cmd: sode kb gaps
    expect: '`kb_ready(architecture)` true'
  depends_on:
  - session-s1-skeleton
- id: session-s10-gate-item-aware
  kind: build_session
  session: S10
  priority: P1
  lane: GREEN
  status: pending
  title: 'S10 — gate item-aware: C7b: brief lint · acceptance script · record completeness · port manifests as checks'
  description: 'Build session S10 (gate item-aware). Builds: C7b: brief lint · acceptance script · record completeness
    · port manifests as checks. Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: BAD fixture per check
  depends_on:
  - session-s5-brief
  - session-s6-gate-core
  - session-s8-record
- id: session-s11-review-red
  kind: build_session
  session: S11
  priority: P1
  lane: RED
  status: pending
  title: 'S11 — review (RED): C8: checker prompt + verdict script · canary corpus + cadence · fixer loop (bounded,
    finding ids) · cumulative pre-fligh'
  description: 'Build session S11 (review (RED)). Builds: C8: checker prompt + verdict script · canary corpus +
    cadence · fixer loop (bounded, finding ids) · cumulative pre-flight · shadow ledger + statistical switch · fan-out
    completeness. Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: evidence-less PASS → UNKNOWN · canary miss disables · cross-family · 0/5 OFF · dead leg → UNKNOWN
  depends_on:
  - session-s8-record
  - session-s6-gate-core
  - session-s9-departments
  - session-s4-worker-runtime
- id: session-s12-cost
  kind: build_session
  session: S12
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S12 — cost: C12: usage readers · headroom chain (Codex exact · Claude port · calibrate) · stop rules ·
    prediction ledger'
  description: 'Build session S12 (cost). Builds: C12: usage readers · headroom chain (Codex exact · Claude port
    · calibrate) · stop rules · prediction ledger. Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: p80 after 10 · HALT/KILL thresholds · stale headroom = hold + notify
  depends_on:
  - session-s4-worker-runtime
- id: session-s13-ship-red
  kind: build_session
  session: S13
  priority: P1
  lane: RED
  status: pending
  title: 'S13 — ship (RED): C10: merge queue · conflict fixer · profiles (booleans) · `stop-all` · revert · notify
    · handoff/resume · governor · con'
  description: 'Build session S13 (ship (RED)). Builds: C10: merge queue · conflict fixer · profiles (booleans)
    · `stop-all` · revert · notify · handoff/resume · governor · control room (`ps/status --live/tail`). Spec: docs/design/sode_factory_component_specs.md;
    order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: stale-base rebased + re-gated · conflict → fixer · GREEN human-free · RED waits · profile matrix
  depends_on:
  - session-s7-engine-red
  - session-s8-record
  - session-s11-review-red
- id: session-s14-claude-adapter
  kind: build_session
  session: S14
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S14 — Claude adapter: C3b: `claude_code_cli` + capability suite + live conformance smoke (manual)'
  description: 'Build session S14 (Claude adapter). Builds: C3b: `claude_code_cli` + capability suite + live conformance
    smoke (manual). Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: recorded-transcript contract tests · hooks installed · capabilities green
  depends_on:
  - session-s4-worker-runtime
  - session-s3-guards-red
- id: session-s15-observe-v1-red
  kind: build_session
  session: S15
  priority: P1
  lane: RED
  status: pending
  title: 'S15 — observe v1 (RED): C13: hash-chained trajectories · evaluator manifest · `active.json` + promote/rollback
    + failure injection · `harness-ca'
  description: 'Build session S15 (observe v1 (RED)). Builds: C13: hash-chained trajectories · evaluator manifest
    · `active.json` + promote/rollback + failure injection · `harness-card` · eval skeleton. Spec: docs/design/sode_factory_component_specs.md;
    order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: content-addressed · tamper → FAIL · atomic promote/rollback · boundary denied · card determinism
  depends_on:
  - session-s8-record
  - session-s12-cost
- id: session-s16-metrics
  kind: build_session
  session: S16
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S16 — metrics: C14: registry (five seeds) · field-binding + compute gates · `sode metrics` · history ·
    alerts'
  description: 'Build session S16 (metrics). Builds: C14: registry (five seeds) · field-binding + compute gates
    · `sode metrics` · history · alerts. Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: registry gate · fixture metric matches · alert opens item
  depends_on:
  - session-s15-observe-v1-red
- id: session-s17-measure
  kind: build_session
  session: S17
  priority: P1
  lane: GREEN
  status: pending
  title: 'S17 — measure: C11: baseline in verify · `measure_queue` · after-read via `sode-run` · verdicts'
  description: 'Build session S17 (measure). Builds: C11: baseline in verify · `measure_queue` · after-read via
    `sode-run` · verdicts. Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: three verdicts · excessive → follow-up · unmeasured alert
  depends_on:
  - session-s13-ship-red
  - session-s16-metrics
- id: session-s18-automations
  kind: build_session
  session: S18
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S18 — automations: C15: trigger runner · crawl set · measure-queue drain · weekly `sode drift` sweep (coverage
    grid · staleness · determini'
  description: 'Build session S18 (automations). Builds: C15: trigger runner · crawl set · measure-queue drain ·
    weekly `sode drift` sweep (coverage grid · staleness · determinism). Spec: docs/design/sode_factory_component_specs.md;
    order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: idempotent re-delivery · `restricted` refuses host scheduler · planted stale entry caught
  depends_on:
  - session-s7-engine-red
  - session-s10-gate-item-aware
  - session-s13-ship-red
  - session-s15-observe-v1-red
  - session-s16-metrics
  - session-s17-measure
- id: session-s19-walk
  kind: build_session
  session: S19
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S19 — WALK: `tests/test_standalone.py` full form + ONE real factory_ops item shipped BY sode end to end
    with a complete record'
  description: 'Build session S19 (WALK). Builds: `tests/test_standalone.py` full form + ONE real factory_ops item
    shipped BY sode end to end with a complete record. Spec: docs/design/sode_factory_component_specs.md; order:
    docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: §1.4 all four criteria green — **v1 is FUNCTIONAL here**
  depends_on: []
- id: session-s20-codex-adapter
  kind: build_session
  session: S20
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S20 — Codex adapter: C3c: `codex_cli` + capability suite; a mock→Codex switch does not regress the eval
    skeleton'
  description: 'Build session S20 (Codex adapter). Builds: C3c: `codex_cli` + capability suite; a mock→Codex switch
    does not regress the eval skeleton. Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: proves the model-agnostic abstraction on real work
  depends_on: []
- id: session-s21-observe-v1-1
  kind: build_session
  session: S21
  priority: P1
  lane: YELLOW
  status: pending
  title: 'S21 — observe v1.1: C13b: scorers (cross-family judge) · `sode replay` · `sode distill`'
  description: 'Build session S21 (observe v1.1). Builds: C13b: scorers (cross-family judge) · `sode replay` · `sode
    distill`. Spec: docs/design/sode_factory_component_specs.md; order: docs/design/sode_factory_build_sessions.md.'
  acceptance:
  - cmd: make test && python3 -m pytest tests/test_standalone.py -q
    expect: replay reproduces a recorded item; distilled entry carries evidence refs
  depends_on: []
- id: seed-b001
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Third-party tooling review: beads (pre-flight + pilot) and alternatives — DEFERRED until after W3; sode
    builds'
  description: 'Third-party tooling review: beads (pre-flight + pilot) and alternatives — DEFERRED until after W3;
    sode builds its native store regardless (review A11). Also carried in this repo''s backlog as `sode-third-party-tooling-review-beads`.'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b002
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Decision-model pre-flight (Sage / Jev): security + ZDR + no-external-API approval.'
  description: 'Decision-model pre-flight (Sage / Jev): security + ZDR + no-external-API approval.'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b003
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Decision-model use-sites, one item each, each stating WHERE it plugs in, HOW it is shadow-tested (agreement
    · '
  description: 'Decision-model use-sites, one item each, each stating WHERE it plugs in, HOW it is shadow-tested
    (agreement · latency · cost vs the current path) and its value add: triage proposals · review-depth routing
    · 100% scorer coverage · auto-ship confidence input · money/auth/secrets diff pre-flag (§6.7).'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b004
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Metric-group discussions, one per group (§9.6): precise definitions · drivers · measures · baselines ·
    alert r'
  description: 'Metric-group discussions, one per group (§9.6): precise definitions · drivers · measures · baselines
    · alert rules → registry entries.'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b005
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Product-plane design session (§10 of the architecture): card schema · risk kernel · re-validation · maintenanc'
  description: 'Product-plane design session (§10 of the architecture): card schema · risk kernel · re-validation
    · maintenance layer.'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b006
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: Container runner backend (when external targets or guard-floor evidence demand it).
  description: Container runner backend (when external targets or guard-floor evidence demand it).
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b007
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: interlinked-cli rule-catalogue comparison vs C4 rules (reference-only).
  description: interlinked-cli rule-catalogue comparison vs C4 rules (reference-only).
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b008
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: Security department KB seeding session (patterns · CVE classes · tooling picks, pinned).
  description: Security department KB seeding session (patterns · CVE classes · tooling picks, pinned).
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b009
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: Gate signal-to-noise audit cadence (per-check catch/FP review) — factory_ops recurring.
  description: Gate signal-to-noise audit cadence (per-check catch/FP review) — factory_ops recurring.
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b010
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: Experience-bank distillation cadence + first distillation after W5.
  description: Experience-bank distillation cadence + first distillation after W5.
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b011
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: Evolve unlock criteria check (≥50 trajectories/stage + hidden holdout) — factory_ops recurring.
  description: Evolve unlock criteria check (≥50 trajectories/stage + hidden holdout) — factory_ops recurring.
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b012
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Claude headroom port: transcript-usage parser + era-scoped rate model + UI% calibration (`sode headroom
    calibr'
  description: 'Claude headroom port: transcript-usage parser + era-scoped rate model + UI% calibration (`sode headroom
    calibrate`); Codex exact-read parser pinned to a rollout fixture (review B10).'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b013
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: Clone GC policy + `sode archive-run` / `import-run` portable evidence bundles (review B19).
  description: Clone GC policy + `sode archive-run` / `import-run` portable evidence bundles (review B19).
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b014
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: Gate-outcomes adjudication cadence (TP/FP labelling of every FAIL) — factory_ops recurring (review E8).
  description: Gate-outcomes adjudication cadence (TP/FP labelling of every FAIL) — factory_ops recurring (review
    E8).
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b015
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: Weekly `sode drift` sweep review — factory_ops recurring (review E14).
  description: Weekly `sode drift` sweep review — factory_ops recurring (review E14).
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b016
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Shadow-phase review: per class × department, when does YELLOW auto-ship flip — factory_ops recurring (review
    B'
  description: 'Shadow-phase review: per class × department, when does YELLOW auto-ship flip — factory_ops recurring
    (review B11).'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b017
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: Formal state model of C2 (TLA+ or equivalent) once the invariant table stabilises (review D5).
  description: Formal state model of C2 (TLA+ or equivalent) once the invariant table stabilises (review D5).
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b018
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'KB SEEDING sessions, one per department (W1–W2, parallel with the code build): security · qa_quality ·
    factory'
  description: 'KB SEEDING sessions, one per department (W1–W2, parallel with the code build): security · qa_quality
    · factory_ops from our own material; backend · frontend · devops from operator-brought articles + best practices;
    architecture from the decision-record template + design sources. Acceptance per session: `kb_ready` flips true
    (every checklist angle covered by a sourced entry + a conventions section). The WALK milestone requires factory_ops
    ready (sode''s first real items are its own).'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b019
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'GAP-1 (apply-check): one clone per ITEM reused across stages vs a fresh clone per stage — decide at C3;
    adviso'
  description: 'GAP-1 (apply-check): one clone per ITEM reused across stages vs a fresh clone per stage — decide
    at C3; advisor recommends one clone per item, fresh only on a PARTIAL continuation after a kill.'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b020
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Apply-check closures GAP-2..GAP-7 (architecture Appendix B): plan-only read-only clone (C3) · T1 runs
    in the c'
  description: 'Apply-check closures GAP-2..GAP-7 (architecture Appendix B): plan-only read-only clone (C3) · T1
    runs in the clone via `sode-run` after worker exit (C7a) · ship approval hash = (diff sha, record sha) (C6)
    · `.sode/measure_queue.jsonl` at ship (C11/C15) · null-safe rates on an empty ledger (spec of the first dogfood
    item) · triage stage body with a fixed output schema (C1/C15).'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b021
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Tool eval: ponytail (minimal-code senior-dev agent) as a sode maker style / skill — baseline vs current
    maker '
  description: 'Tool eval: ponytail (minimal-code senior-dev agent) as a sode maker style / skill — baseline vs
    current maker on 2–3 real script tasks; correctness · tokens · review burden; security pre-flight before install
    (operator 2026-08-22).'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b022
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Evolve reference: Meta-Harness / harness-evolver (7-stage evolve loop, isolated proposers, counterfactual
    trac'
  description: 'Evolve reference: Meta-Harness / harness-evolver (7-stage evolve loop, isolated proposers, counterfactual
    trace diagnosis) as the design reference for §7.5 when it unlocks — reference-only, no install.'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b023
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Semantic-escape design session: "what semantic error passes ALL gates?" — rule-of-five perspective-diverse
    rev'
  description: 'Semantic-escape design session: "what semantic error passes ALL gates?" — rule-of-five perspective-diverse
    review, explain-a-sample, surrogate verifier; RED; design-first with deterministic catch + alarm per mitigation.'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b024
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Runner hardening eval: isolation ladder (container < gVisor < microVM < VM) vs OS syscall jail (Seatbelt/Landl'
  description: 'Runner hardening eval: isolation ladder (container < gVisor < microVM < VM) vs OS syscall jail (Seatbelt/Landlock)
    for the `container` runner + egress-proxy credential injection — evaluate before the first external target.'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
- id: seed-b025
  kind: backlog_seed
  priority: P2
  lane: YELLOW
  status: pending
  title: 'Gap-closure cadence: `sode kb gaps` + worker confusions with no KB coverage → factory_ops items; first
    review '
  description: 'Gap-closure cadence: `sode kb gaps` + worker confusions with no KB coverage → factory_ops items;
    first review after W5 — self-learning fills the `run_lesson` layer over time (operator 2026-09-18).'
  acceptance:
  - cmd: 'true'
    expect: resolved per the seed text; recorded in the architecture doc or a promoted ADR
